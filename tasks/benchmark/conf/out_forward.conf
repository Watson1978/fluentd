<source>
  @type tail
  tag test
  path "#{File.expand_path './tmp/benchmark/data.log'}"
  read_from_head true
  <parse>
    @type json
  </parse>
</source>

<match test.**>
  @type forward
  compress zstd
  # compress gzip

  <server>
    host 127.0.0.1
    port 24224
  </server>

</match>